name: Auto Tag and Release

on:
  push:
    branches:
      - main

jobs:
  auto-tag-and-release:
    name: Build & Auto Publish Release
    runs-on: windows-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Get Version from package.json
        id: package_version
        shell: powershell
        run: |
          $pkg = Get-Content package.json | ConvertFrom-Json
          $ver = "v" + $pkg.version
          "VERSION=$ver" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "version=$ver" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Check if Tag Already Exists
        id: check_tag
        shell: powershell
        run: |
          git fetch --tags
          $targetTag = "$env:VERSION"
          $exists = git tag -l $targetTag
          if ($exists) {
            "exists=true" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
            Write-Host "Tag $targetTag already exists. Skipping release build."
          } else {
            "exists=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          }

      - name: Create and Push Git Tag
        if: steps.check_tag.outputs.exists == 'false'
        shell: powershell
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag $env:VERSION
          git push origin $env:VERSION

      - name: Install dependencies
        if: steps.check_tag.outputs.exists == 'false'
        run: npm ci

      - name: Build Vite Frontend
        if: steps.check_tag.outputs.exists == 'false'
        run: npm run build

      - name: Build and Publish Electron App
        if: steps.check_tag.outputs.exists == 'false'
        run: npx electron-builder --win --publish always -c.publish.releaseType=release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
